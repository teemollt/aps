# 리스트 큐
queue = list()

queue.append()
queue.pop(0)

# 라이브러리
import queue

#
class MyQueue:
    def __init__(self):
        # front: 현재 큐에 들어있는 요소 중 가장 앞요소
        # rear: 가장 마지막 요소
        # 최대 저장길이는 10
        pass
    # enQueue(): queue에 요소를 넣는 기능
        # rear의 뒤에 새로운 요소 추가
    # deQueue(): queue의 front를 반환하고 삭제
    # isEmpty(): queue가 비어있는지 확인
        # 비어있으면 True / 아니면 False
    # isFull(): queue가 가득 찼는지 확인
        # 가득차 있으면 True / 아니면 False
    # Qpeek(): queue의 front를 반환

